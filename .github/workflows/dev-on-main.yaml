name: dev-on-main Branch 🐧
run-name: ${{ github.actor }} dev-on-main 🚀
on:
  push:
    branches:
      - main
jobs:
  dev-on-main-Branch:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 Push on dev branch by a ${{ github.event_name }} event."
