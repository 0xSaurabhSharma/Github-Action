name: Push On CICD Branch 🐧
run-name: ${{ github.actor }} cicd-another-workflow 🚀
on:
  push:
    branches:
      - 'cicd/*'
jobs:
  Push-On-CICD-Branch:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 push on cicd brach ${{ github.event_name }} event."