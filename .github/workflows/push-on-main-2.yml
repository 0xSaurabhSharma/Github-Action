name: push-on-main-2 🐧
run-name: ${{ github.actor }} push-on-main-2 🚀
on:
  push:
    branches: [ main ]
jobs:
  push-on-main-2-Branch:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 Push on main branch by a ${{ github.event_name }} event."
