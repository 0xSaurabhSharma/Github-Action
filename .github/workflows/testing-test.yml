name: testing-test Branch 🐧
run-name: ${{ github.actor }} testing-test 🚀
on:
  push
jobs:
  testing-test-Branch:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 Push on dev branch by a ${{ github.event_name }} event."