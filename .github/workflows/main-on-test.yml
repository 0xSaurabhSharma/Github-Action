name: main-on-test Branch 🐧
run-name: ${{ github.actor }} main-on-test 🚀
on:
  push:
    branches:
      - test
jobs:
  main-on-test-Branch:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 Push on dev branch by a ${{ github.event_name }} event."
