name: test-on-main Branch 🐧
run-name: ${{ github.actor }} test-on-main 🚀
on:
  push:
    branches:
      - main
jobs:
  test-on-main-Branch:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 Push on dev branch by a ${{ github.event_name }} event."
