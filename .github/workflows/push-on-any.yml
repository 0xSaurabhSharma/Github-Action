name: Push On ANY Branch 🐧
run-name: ${{ github.actor }} is push on ANY 🚀


on:
  push:
    branches: [ "*" ]  # triggers on any push to any branch

jobs:
  Push-On-ANY-Branch:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 On ANY brach by a ${{ github.event_name }} event."
