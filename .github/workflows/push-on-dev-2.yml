name: push-on-dev-2 Branch 🐧
run-name: ${{ github.actor }} push-on-dev-2 🚀
on:
  push:
    branches:
      - 'dev'
      - main
      - '!test/*'
      - '!dev'
jobs:
  push-on-dev-2-Branch:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 Push on dev branch by a ${{ github.event_name }} event."

