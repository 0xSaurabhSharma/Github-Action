name: main-on-main Branch 🐧
run-name: ${{ github.actor }} main-on-main 🚀
on:
  push:
    branches:
      - main
jobs:
  main-on-main-Branch:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 Push on dev branch by a ${{ github.event_name }} event."
